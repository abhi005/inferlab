[project]
name = "inferlab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "abhi005", email = "jainabhi446@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "torch>=2.13.0",
    "transformers>=5.14.1",
]

[project.scripts]
inferlab = "inferlab:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
